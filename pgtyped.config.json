{
	"transforms": [
		{
			"mode": "sql",
			"include": "**/*.sql",
			"emitTemplate": "{{dir}}/{{name}}.queries.ts"
		}
	],
	"srcDir": "./src/db/queries",
	"dbUrl": "postgres://postgres:password@localhost/postgres"
}
